using UnityEngine;

public class InventoryCanva : MonoBehaviour
{
    public static InventoryCanva instance;

    private void Awake()
    {
        if (instance != null && instance != this)
        {
            Destroy(gameObject);
            return;
        }

        instance = this;
        DontDestroyOnLoad(gameObject);
    }
}
